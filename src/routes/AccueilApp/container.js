import {connect} from 'react-redux';
import Accueil from '../AccueilApp/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(Accueil);