import {connect} from 'react-redux';
import Modal from '../component/Modal';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(Modal);