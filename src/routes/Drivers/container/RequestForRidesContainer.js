import {connect} from 'react-redux';
import RequestForRides from '../component/RequestForRides';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(RequestForRides);