import {connect} from 'react-redux';
import StopActivity from '../component/StopActivity';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(StopActivity);