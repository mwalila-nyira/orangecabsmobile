import {connect} from 'react-redux';
import ProfileDriver from '../component/Profile';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(ProfileDriver);