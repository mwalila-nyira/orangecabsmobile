import {connect} from 'react-redux';
import TripComplete from '../component/TripComplete';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(TripComplete);