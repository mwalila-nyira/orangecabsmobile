import {connect} from 'react-redux';
import Driver from '../component/Driver';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(Driver);