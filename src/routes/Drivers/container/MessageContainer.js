import {connect} from 'react-redux';
import MessageDriver from '../component/Message';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(MessageDriver);