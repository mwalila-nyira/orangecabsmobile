import {connect} from 'react-redux';
import forgotpass from '../ForgotPassword/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(forgotpass);