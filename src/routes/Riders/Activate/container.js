import {connect} from 'react-redux';
import activate from '../Activate/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(activate);