import {connect} from 'react-redux';
import signup from '../Signup/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(signup);