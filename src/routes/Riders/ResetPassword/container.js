import {connect} from 'react-redux';
import reset from '../ResetPassword/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(reset);