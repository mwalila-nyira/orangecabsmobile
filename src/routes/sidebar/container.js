import {connect} from 'react-redux';
import sidebar from '../sidebar/index';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(sidebar);