import {connect} from 'react-redux';
import Message from '../component/Message';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(Message);