import {connect} from 'react-redux';
import ViewTrip from '../component/ViewTrip';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(ViewTrip);