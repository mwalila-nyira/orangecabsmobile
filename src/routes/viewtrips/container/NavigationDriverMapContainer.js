import {connect} from 'react-redux';
import NavigationDriverMap from '../component/NavigationDriverMap';

const mapStateTopProps = (state) => ({

});

const mapActionCreators = {

};

export default connect(mapStateTopProps,mapActionCreators)(NavigationDriverMap);